Arabic dictionary source: AyaSpell 3.7.

This project uses the AyaSpell Hunspell dictionary as the source for the
bundled Arabic keyboard lexicon. The build tool keeps all clean source entries
and generates the direct prefix/suffix derivatives allowed by AyaSpell flags.
The resulting runtime files are split into compressed prefix buckets.

Original authorship and license files are preserved in this directory.
