# PROJECT_JOURNAL

## Final Base الحالية

- `Patch 23 — Arabic Common Word Ranking Layer` هي Final Base الحالية والوحيدة.
- أي عمل لاحق يبنى على `main` بعد Patch 23 فقط.
- `Patch 24 — Companion App Visual Foundation` مرشح قيد الاختبار، ولا يصبح Final Base قبل تأكيد المستخدم: `تم التطبيق بنجاح`.

## المرحلة الحالية

- الانتقال من تحسين القواميس إلى بناء تجربة منتج واضحة وحديثة للإصلاح المباشر داخل لوحة المفاتيح.
- الفرع النشط: `agent/patch-24-companion-app-visual-foundation`.
- نطاق Patch 24 هو التطبيق المرافق ومسار التفعيل والمعاينة البصرية فقط؛ لا يغيّر محرك القواميس أو سطح لوحة Android الأصلي أو نظام المكافآت الفعلي.

## Patch 23 — Arabic Common Word Ranking Layer

- إضافة طبقة محلية خفيفة تضم `10,442` كلمة عربية شائعة مرتبة حسب تكرارها في FrequencyWords.
- اعتماد أول `30,000` مدخل من `ar_50k.txt` بعد إزالة الرموز والتكرارات والضجيج، ثم قبول الكلمات المطابقة مباشرةً لقاموس AyaSpell فقط.
- تقديم الكلمات الشائعة داخل نتائج البادئة، مع بقاء قاموس AyaSpell الكبير مصدرًا احتياطيًا كاملًا للكلمات والتصريفات الأقل شيوعًا.
- إضافة فهرس صغير لأفضل اقتراحات الحرف الأول، مع تحميل طبقة العربية عند الحاجة فقط.
- الإبقاء على التعلم المحلي في موضعه الأعلى، وعدم تغيير الإنجليزية أو الفرنسية أو شريط الإصلاح أو تصميم لوحة المفاتيح.
- توثيق FrequencyWords وترخيص المحتوى `CC BY-SA 4.0` وترخيص الكود `MIT` مركزيًا، مع حفظ إشعار ترخيص AyaSpell منفصلًا.

## 2026-08-01 — اتجاه المنتج المعتمد

### الهوية والتصميم

- لا نقلد أي لوحة مفاتيح أخرى تقليدًا أعمى؛ نستفيد فقط من الأفكار التي تجعل التجربة أسرع وأوضح وأجمل.
- التطبيق عربي وعالمي، وليس موجهًا إلى سوق أو ثقافة واحدة.
- الأولوية في البداية لواجهة خفيفة، واضحة، سريعة، ومركزة على إصلاح العربية.
- شريط الأدوات يبقى بسيطًا قدر الإمكان: العصا السحرية، الحافظة، الرموز التعبيرية، تغيير اللغة، والإعدادات.
- الميزات غير الأساسية مثل الملصقات، الكتابة اليدوية، حفظ حالات التطبيقات، الأخبار والنتائج، والمؤثرات الكثيرة ليست من أولوية البداية.

### ملاحظات التصميم المستخلصة من الصور المرجعية

- اعتماد مسار تفعيل مرئي خطوة بخطوة: التفعيل، الاختيار، ثم الاختبار، مع حالة واضحة لكل مرحلة.
- توفير معاينة حقيقية لشكل لوحة المفاتيح قبل تطبيق أي تخصيص، بدل الاكتفاء بأسماء خيارات مجردة.
- تنظيم التطبيق في ثلاثة أقسام رئيسية واضحة: لوحة المفاتيح، المكافآت، الإعدادات.
- تنظيم الإعدادات في بطاقات كبيرة بحواف مستديرة، عنوان واضح، وصف قصير، وفواصل خفيفة بدل قائمة تقنية مزدحمة.
- تمييز أزرار الوظائف بصريًا عن مفاتيح الحروف، مع زر إدخال بارز وزر مسافة واسع ومسافات منتظمة بين المفاتيح.
- دعم الوضع الفاتح والداكن لاحقًا من الأساس نفسه، مع خطة للتباين العالي دون إنشاء تصميم منفصل متناقض.
- توفير زر ومساحة اختبار سريعة من داخل التطبيق حتى لا يحتاج المستخدم إلى فتح تطبيق خارجي بعد كل تغيير.
- إعدادات المظهر المستقبلية ذات الأولوية: ارتفاع اللوحة، حجم الحروف، المسافة السفلية، حدود المفاتيح، الصوت، الاهتزاز، والتباين.
- الإيماءات المفيدة مثل تحريك المؤشر من زر المسافة والحذف بالسحب تدرس تقنيًا قبل تفعيلها؛ لا تضاف كعناصر شكلية غير عاملة.
- إدارة البيانات المحلية والقواميس والتعلم يجب أن تظهر لاحقًا داخل قسم واضح، مع شرح ما يحذف وما يبقى.
- لا ننقل الملصقات، حفظ حالات التطبيقات، النتائج، الأخبار، الكتابة اليدوية، أو كثرة المؤثرات إلى المرحلة الأولى.
- الثيمات تبقى داخل قسم المظهر في البداية، ولا تأخذ تبويبًا مستقلًا على حساب المكافآت أو الإعدادات الأساسية.

### تجربة العصا السحرية

- العصا السحرية هي الميزة الرئيسية للمنتج.
- عند الضغط عليها يظهر داخل لوحة المفاتيح، فوق صفوف المفاتيح، إطار كتابة مدمج يشبه من حيث المبدأ تجربة الترجمة في Gboard.
- يكتب المستخدم داخل هذا الإطار عربية طبيعية متصلة كما يريد أن يراها.
- لا نستخدم سير عمل «نسخ ثم تحويل»، ولا نطلب من المستخدم فتح تطبيق منفصل أو لصق النص يدويًا.
- أثناء الكتابة يبقى حقل التطبيق الهدف غير ممتلئ قدر الإمكان، وتتم إدارة النص داخل لوحة المفاتيح نفسها.
- عند ضغط المستخدم على الإرسال أو البحث أو الإجراء المناسب للتطبيق الهدف، تحول لوحة المفاتيح النص محليًا وبصورة غير مرئية إلى الصيغة التي تعمل في التطبيقات التي تعرض العربية مقطعة أو معكوسة، ثم تدرجه وتنفذ الإجراء المناسب.
- المستخدم يجب أن يرى العربية الطبيعية فقط، ولا يرى النص التقني المحول أو الحروف المهيأة للتوافق.
- بعد نجاح الإرسال يمسح إطار العصا ويصبح جاهزًا لرسالة جديدة.
- عند إغلاق وضع العصا تعود لوحة المفاتيح العادية وشريط الاقتراحات دون تعطيل.
- الكتابة العادية تبقى متاحة دائمًا، حتى عند نفاد رصيد العصا.

### الخصوصية والتحليلات

- المعالجة تبقى محلية على الجهاز ما دام ذلك ممكنًا.
- يمنع إرسال نصوص المستخدم إلى خادم أو تسجيلها في Analytics أو Crashlytics.
- الأحداث المسموح بها تكون وصفية فقط، مثل فتح العصا، نجاح الإرسال، فشله، نفاد الرصيد، وفتح صفحة المكافآت، دون محتوى الرسالة.

### نظام المكافآت والإعلانات

- كان البيع داخل التطبيق خيارًا تجاريًا مرغوبًا، لكن المستخدم لا يستطيع حاليًا الاعتماد عليه من وضعه الحالي في المغرب؛ لذلك اتجاه الربح الحالي هو الإعلانات.
- نظام المكافآت يوجد داخل تطبيق لوحة المفاتيح المرافق، وليس داخل سطح لوحة المفاتيح نفسها.
- يمنع إظهار إعلان داخل مساحة الكتابة أو فوق المفاتيح أثناء الاستعمال.
- الإعلان الأساسي المقترح هو Rewarded Ad اختياري يشاهده المستخدم داخل التطبيق للحصول على رصيد إضافي للعصا السحرية.
- صفحة المكافآت تعرض الرصيد الحالي، الاستخدامات المجانية المتبقية، زر مشاهدة إعلان مكافأة، وشرحًا بسيطًا لطريقة الاستهلاك بعد اعتماد القيم وربط AdMob.
- الاتجاه الحالي: كل إرسال ناجح عبر العصا يستهلك استخدامًا واحدًا، ولا يخصم شيء عند فشل الإدراج أو الإرسال.
- عند نفاد الرصيد، تبقى لوحة المفاتيح العادية عاملة، وتوجه العصا المستخدم بوضوح إلى تطبيق المكافآت دون تعطيل الكتابة.
- عدد الاستخدامات المجانية اليومية، مقدار مكافأة الإعلان، الحد الأقصى للرصيد، وقواعد إعادة التعبئة لم تُحسم بعد ويجب اعتمادها قبل التنفيذ.
- أي أشكال إعلانية أخرى داخل التطبيق المرافق تدرس لاحقًا وفق تجربة المستخدم وسياسات AdMob، ولا تضاف تلقائيًا إلى مسار العصا أو الإرسال.

### تنظيم التطبيق المرافق

الاتجاه المعتمد للتبويبات:

- لوحة المفاتيح.
- المكافآت.
- الإعدادات.

داخل الإعدادات يرتب لاحقًا:

- المظهر والثيمات.
- ارتفاع لوحة المفاتيح وحجم الحروف والمسافة السفلية وحدود المفاتيح.
- الصوت والاهتزاز.
- التباين العالي وإمكانية الوصول.
- الإيماءات المفيدة بعد دراسة التنفيذ.

### التفعيل والثقة

- مسار التفعيل واضح خطوة بخطوة: تفعيل لوحة المفاتيح، اختيارها، ثم اختبارها داخل التطبيق.
- يجب شرح الخصوصية للمستخدم بوضوح، لأن Android يعرض تحذيرًا عامًا لأي لوحة مفاتيح خارجية.
- لا ندعي وعودًا غير دقيقة؛ نشرح ببساطة ما نعالجه محليًا وما لا نجمعه.

## Patch 24 — Companion App Visual Foundation (Candidate)

- إنشاء نظام بصري Material 3 أكثر نضجًا: خلفية هادئة، بطاقات مستديرة، أزرار وحقول موحدة، وتنقل سفلي واضح.
- إعادة بناء الصفحة الرئيسية حول ميزة العصا السحرية بدل بطاقات عامة لا تشرح قيمة المنتج.
- اعتماد ثلاثة تبويبات فقط: لوحة المفاتيح، المكافآت، الإعدادات.
- إضافة معاينة Responsive للوحة العربية وشريط أدواتها، مستلهمة من الصور دون نسخ هوية التطبيق المرجعي.
- إعادة بناء صفحة التفعيل في ثلاث خطوات واضحة مع شرح تحذير Android والخصوصية ومربع اختبار داخلي.
- إعادة تنظيم الإعدادات إلى مجموعات كتابة ومظهر وراحة، مع الحفاظ على الخيارات العاملة وعدم إنشاء مفاتيح وهمية.
- إنشاء أساس بصري صادق لصفحة المكافآت؛ زر الإعلان معطل بوضوح حتى يتم ربط AdMob واعتماد القيم في Patch مستقل.
- إضافة نصوص عربية وإنجليزية وفرنسية للتجربة الجديدة.
- تحديث اختبار الواجهة لتغطية بدء التطبيق والتنقل بين التبويبات الثلاثة.
- لا تغيير في `KeyboardImeService` أو القواميس أو ترتيب الاقتراحات أو منطق الإصلاح أو الرصيد الفعلي في هذا الباتش.

## قيود ومخاطر يجب ألا تُنسى

- يجب التحقق تقنيًا من إمكانية اعتراض زر الإرسال وتنفيذ الإجراء المناسب بصورة مستقرة عبر تطبيقات مختلفة؛ لا يفترض أن جميع التطبيقات تتصرف بالطريقة نفسها.
- يجب توفير Fallback آمن عندما يمنع التطبيق الهدف الإدراج أو الإرسال التلقائي، دون فقدان النص أو خصم الرصيد.
- لا يثبت أي عدد للمكافآت أو معدل استهلاك نهائي دون مراجعة الربح وتجربة المستخدم وسياسة الإعلانات.
- تخصيصات المظهر لا تعرض كمفاتيح عاملة قبل ربطها فعليًا بسطح لوحة Android.

## ما يجب اختباره في Patch 24

- تشغيل `flutter analyze` و`flutter test`.
- فحص الصفحة الرئيسية والتبويبات الثلاثة بالعربية والإنجليزية والفرنسية.
- فحص الوضعين الفاتح والداكن وحجم الخط الكبير.
- فحص المعاينة على شاشة ضيقة والتأكد من غياب Overflow.
- فتح إعدادات Android واختيار لوحة المفاتيح من صفحة التفعيل ثم الكتابة داخل مربع الاختبار.
- التأكد أن لوحة المفاتيح الأصلية ومحرك القواميس لم يتغير سلوكهما.

## الخطوة التالية بعد اعتماد Patch 24

- بناء المرحلة البصرية التالية على سطح `KeyboardImeService` نفسه: المفاتيح، شريط الأدوات، التمييز بين الوظائف والحروف، والقياسات المتكيفة.
- بعدها يدرس مسار الإرسال السحري وFallback في Patch مستقل قبل إضافة AdMob والمكافآت الفعلية.
