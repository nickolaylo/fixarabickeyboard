# PROJECT_JOURNAL — Fix Arabic Keyboard

> **بوابة الدخول التشغيلية للمشروع وفق GWC-061 — آخر تدقيق: 2026-08-08**
>
> ابدأ من هذا الملف، ثم تحقق من Git الفعلي والمراجع أدناه. لا تطلب من المستخدم إعادة شرح تاريخ المشروع أو الميثاق أو المستودعات. عند التعارض، الحقيقة الأحدث في Git مقدمة على النص القديم، ثم يُصحح الجورنال.

## 1) تعريف المشروع والهدف

- المستودع: `nickolaylo/fixarabickeyboard`.
- المنصة الأساسية الحالية: Android.
- البنية: تطبيق Flutter مرافق + لوحة مفاتيح Android أصلية مبنية على `InputMethodService`.
- الحزمة: `com.souadachak.fixarabickeyboard`.
- الهدف الرئيسي: حل مشكلة ظهور العربية مقطعة/معكوسة/غير متصلة داخل بعض الألعاب والتطبيقات، من داخل لوحة المفاتيح نفسها.
- تجربة المنتج المعتمدة: يكتب المستخدم العربية الطبيعية داخل حقل العصا السحرية، ثم يُحوّل النص محليًا ويُدرج في التطبيق الهدف دون سير عمل يدوي من نوع فتح تطبيق منفصل → تحويل → نسخ → لصق.
- العربية هي محور المنتج، مع لوحة واقتراحات محلية بالعربية والإنجليزية والفرنسية.

### تنبيه جوهري: محرك الإصلاح الحقيقي لم يُبنَ بعد داخل لوحة المفاتيح

`android/app/src/main/kotlin/com/souadachak/fixarabickeyboard/keyboard/CorrectionEngine.kt` ما يزال Placeholder بسيطًا فقط: ينظف المسافات ويستبدل `اا` بـ`ا`. لا يعالج حاليًا مشكلة تشكيل/عكس الحروف العربية التي صُمم المنتج من أجلها.

المرجع العملي الحالي للمحرك الأصلي هو:

- المستودع: `nickolaylo/fix_arabic2026`
- الملف: `lib/models/converter.dart`
- نقطة الاستعمال في التطبيق الأصلي: `lib/screens/message_editing_screen_v3.dart`

عند بدء مرحلة الإصلاح الحقيقي يجب دراسة هذا المحرك ونقل فكرته/منطقه بصورة محافظة إلى Android/Kotlin مع اختبارات ثابتة، لا إعادة اختراع الحل من الصفر ولا نسخه أعمى دون اختبار النصوص المختلطة والأسطر وعلامات الترقيم.

## 2) الحقيقة الحالية في Git

### Final Base الوحيدة

- **Patch 33 — Contextual Cold-Start Dictionary Quality** هي `Final Base` الحالية.
- دمجت عبر PR #12 في `main` بتاريخ 2026-08-03.
- Commit دمج المنتج المرجعي: `ee618096954774b034f55ac422b507890b2424eb`.
- كل ما قبل Patch 33 مدمج فيها ولا يُبنى مستقبلًا على Patch أقدم.

### حالة `main`

- الفرع الرئيسي الفعلي: `main`.
- بعد دمج Patch 33 توجد على `main` مجموعة Commits مؤقتة خاصة بإضافة/حذف Workflows للتحقق من Patch 34؛ لا تمثل Final Base منتجية جديدة.
- عند تدقيق 2026-08-08 كان رأس `main` قبل تحديث هذا الجورنال هو `fe6b57a160003c201d69f9b68b9694184e12aae4`، بينما Merge Base المنتجية مع الفرع النشط بقيت Patch 33 (`ee6180...`).

### العمل النشط

- **Patch 34 — Keyboard Personalization & Compose Viewport**.
- الفرع: `agent/patch-34-keyboard-personalization-and-compose-viewport`.
- PR #13: **Open + Draft + غير مدمجة**.
- عند التدقيق كانت PR قابلة للدمج تقنيًا، لكن لا يجوز دمجها قبل قبول نتيجة الاختبار.
- قبل مزامنة هذا الجورنال كان رأس الفرع: `92b6ce74996bd444eabcc40e16e5fae1b14e034c`.
- مقارنة الفرع مع `main` عند التدقيق: `ahead 32 / behind 15`، وCommits الـ15 المتأخرة في `main` هي دورة Workflows مؤقتة بعد Patch 33 وليست تغييرات منتجية يجب البناء عليها يدويًا.
- فرق المنتج في PR #13 محصور في 9 ملفات: الجورنال، Manifest/Styles، `MainActivity.kt`، `KeyboardImeService.kt`، إعدادات/ترجمة Flutter واختبار Widget.

### حالة اختبار Patch 34

موجود في الفرع المرشح:

- إعداد ارتفاع لوحة المفاتيح `85%..115%`.
- إعداد حجم الحروف `85%..120%`.
- إعداد المسافة السفلية `0..24dp`.
- إظهار/إخفاء حدود المفاتيح.
- حفظ الإعدادات في تفضيلات مشتركة بين التطبيق المرافق والـIME.
- حقل تحويل يسمح بعدة أسطر منطقية مع نافذة عرض مستهدفة من سطرين وتمرير نحو المؤشر.
- حساب Safe Insets لمنع أزرار/إيماءات Android من التداخل مع الصف السفلي.

المشكلة المفتوحة داخل Patch 34:

- الشريط السفلي الأصلي الخاص بـAndroid ما زال يظهر أسود على المحاكي بدل الاندماج بصريًا مع خلفية اللوحة.
- جُرب تلوين شريط التنقل عبر Theme، ثم `Theme.InputMethod`، ولم يظهر فرق في آخر اختبار مؤكد من المستخدم.
- أحدث محاولة موجودة حاليًا في الفرع تجعل `navigationBarColor` و`navigationBarDividerColor` شفافين داخل `KeyboardImeTheme` مع خلفية IME بنفس لون اللوحة؛ **لم يصل بعد تقرير المستخدم عن نتيجة هذه المحاولة بعد تحديث 92b6ce...**.
- لا يوجد حاليًا في `KeyboardImeService.kt` تنفيذ Runtime فعلي يضبط نافذة شريط النظام عبر Window API، رغم مناقشة هذا الاتجاه سابقًا؛ لا تفترض أنه طُبق.
- عبارة المستخدم «تم تطبيق الباتش بنجاح لكن لا جديد» كانت تصف نجاح التثبيت/التشغيل مع بقاء المشكلة المرئية، وليست قبولًا لنتيجة Patch 34 كـFinal Base.

**ممنوع دمج Patch 34 أو اعتبارها Final Base قبل إغلاق هذه الملاحظة وبقية اختبارها وقبول المستخدم.**

## 3) الخطوة التالية الواضحة

1. استكمال Patch 34 فقط، بدءًا من نتيجة محاولة الشريط الشفاف الحالية.
2. إذا بقي الشريط أسود: فحص سلوك نافذة `InputMethodService` الفعلي وWindow/Insets على Android الحديث، مع إبقاء الشريط وأزرار الإخفاء/التبديل مملوكة للنظام 100%؛ لا إنشاء شريط بديل داخل اللوحة.
3. اختبار إعدادات Patch 34 وحقل الأسطر على هاتف/تابلت وفي Portrait/Landscape.
4. بعد قبول المستخدم: دمج Patch 34 وتصبح Final Base الجديدة وفق الميثاق.
5. **الأولوية المنتجية الكبرى مباشرة بعد إغلاق Patch 34: بناء محرك الإصلاح العربي الحقيقي** بالاستفادة من `fix_arabic2026/lib/models/converter.dart`، مع اختبارات قبل توسيع ميزات ثانوية.

### نطاق مرحلة الإصلاح التالية

يجب أن تغطي على الأقل:

- العربية الطبيعية → الصيغة المناسبة للتطبيقات التي لا تدعم العربية جيدًا.
- الحروف المتصلة والمنفصلة وحالات بداية/وسط/نهاية الكلمة.
- اللام ألف وأشكال الهمزة والتشكيل.
- علامات الترقيم والأرقام والمسافات.
- الأسطر المتعددة.
- النص المختلط عربي/لاتيني.
- عدم إفساد نص لا يحتاج إلى تغيير.
- إدراج النتيجة في التطبيق الهدف وفشل الإدراج بأمان دون فقدان النص أو خصم الرصيد.
- اختبارات Golden/Unit ثابتة للمحرك قبل توصيله نهائيًا بالعصا.

## 4) خريطة البنية الحالية

### التطبيق المرافق — Flutter

- نقطة الدخول: `lib/main.dart`.
- البنية الرئيسية: `lib/app/`, `lib/core/`, `lib/features/`, `lib/shared/`.
- الميزات الحالية داخل `lib/features/`: `activation`, `home`, `keyboard_settings`, `premium`, `rewards`.
- الربط مع Android والإعدادات المشتركة يمر عبر `MainActivity.kt` وMethodChannel وتفضيلات `keyboard_ui_state`.

### لوحة Android الأصلية

المجلد الأساسي:

`android/app/src/main/kotlin/com/souadachak/fixarabickeyboard/keyboard/`

أهم الملفات:

- `KeyboardImeService.kt`: واجهة اللوحة، الإدخال، الأدوات، الإيموجي، الحذف، حقل العصا، الـInsets.
- `SuggestionEngine.kt`: منطق الاقتراحات.
- `DictionaryManager.kt`: القواميس المحلية وترتيب نتائجها.
- `LocalNextWordStore.kt`: ذاكرة الكلمات/السياق المحلية.
- `CorrectionEngine.kt`: Placeholder حاليًا؛ ليس محرك Fix Arabic الحقيقي.
- `CoinManager.kt`: Wallet محلي قديم/مؤقت؛ أرقامه ليست قرارًا تجاريًا معتمدًا.

### القواميس والتراخيص

- الأصول: `android/app/src/main/assets/dictionaries/`.
- توجد طبقات عربية/إنجليزية/فرنسية وأدوات بناء تحت `tools/dictionaries/`.
- FrequencyWords له Attribution/License محفوظان في مجلد القواميس.
- Ayaspell يحتفظ بترخيصه وإشعاراته المنفصلة داخل `android/app/src/main/assets/dictionaries/licenses/`.
- لا تحذف أو تستبدل ملفات التراخيص عند تحسين القواميس.

### الاعتماديات والخدمات الفعلية الآن

- `pubspec.yaml` الحالي لا يحتوي AdMob أو Firebase؛ يعتمد أساسًا على Flutter/localizations/cupertino.
- لذلك AdMob/Firebase/Rewarded Analytics **خطط منتج وليست تكاملات فعلية في هذه النسخة حتى الآن**.
- جزء `InputMethodService` أصلي ولا يكفي `flutter test` لتغطيته؛ الاختبار على Android إلزامي.

## 5) القرارات المعتمدة التي تقيد التنفيذ

### تجربة المنتج

- لا سير عمل يدوي دائم: فتح تطبيق Fix Arabic → تحويل → نسخ → لصق. العصا السحرية داخل الـIME هي المسار المستهدف.
- النص التقني المحول لا يعرض للمستخدم؛ يكتب العربية الطبيعية وتتم المعالجة محليًا عند التنفيذ.
- الكتابة العادية في اللوحة مجانية ومتاحة دائمًا.
- عند فشل الإصلاح/الإدراج يجب الحفاظ على النص وعدم خصم استخدام.

### القاموس والذاكرة

- القواميس محلية للعربية والإنجليزية والفرنسية.
- بعد Patch 33 لا نعود إلى fallback ثابت من نوع `مرحبا — السلام — شكرا` بعد كل كلمة.
- بعد إكمال كلمة يبقى شريط الاقتراحات مفيدًا: ذاكرة سياقية عند توفرها + كلمات شائعة حقيقية.
- بطاقة الذاكرة تبدأ من جهة الكتابة: يمين العربية، ويسار الإنجليزية/الفرنسية.
- أثناء كتابة بداية كلمة تبقى إكمالات البادئة الحقيقية مع الاقتراح المتعلم المطابق.

### واجهة اللوحة

- الاستفادة من Gboard أو أي لوحة مرجعية تكون في ergonomics والتنظيم فقط، **لا تقليدًا أعمى**.
- شريط الأدوات يبقى بسيطًا ويتكيف مع المساحة؛ الأدوات الزائدة تذهب إلى «المزيد» بدل ازدحام الشريط.
- إعداد صف الأرقام له ثلاث حالات: دائمًا / في Portrait فقط / مخفي؛ الافتراضي المعتمد هو Portrait فقط.
- زر حذف الإيموجي يبقى متاحًا داخل صفحة الإيموجي نفسها.
- إغلاق وضع التحويل يمسح المسودة؛ هذا هو السلوك الأحدث بعد Patch 30، وقد استبدل قرار Patch 28 القديم الذي كان يحتفظ بالمسودة.

### شريط Android السفلي

- أزرار إخفاء لوحة المفاتيح وتبديل طريقة الإدخال وشريط الإيماءات **ملك Android**.
- لا ننشئ View سفليًا يقلدها، ولا نكررها داخل لوحتنا، ولا نخلط شريطًا مصطنعًا مع شريط النظام.
- لا نعتمد حيل إخفاء قسري قد تكسر الإيماءات أو التبديل؛ الهدف هو احترام Safe Area ودمج شريط النظام بصريًا فقط عندما يسمح Android بذلك.

### الخصوصية والربح

- لا إعلانات داخل مساحة الـIME نفسها ولا أثناء الكتابة أو الإرسال.
- اتجاه الربح المعتمد حاليًا: الإعلانات في التطبيق المرافق، وRewarded Ads اختياري لفتح/زيادة استخدام العصا لاحقًا.
- لا ترسل نصوص المستخدم أو محتوى الحافظة إلى خادم الناشر أو Analytics/Crashlytics.
- المعالجة والتعلم والذاكرة محلية ما أمكن.
- أرقام المكافآت/الاستخدامات المجانية/الحد الأقصى **لم تعتمد بعد**.
- الموجود حاليًا في `CoinManager.kt` (`DAILY_FREE_COINS = 100` و`premium`) لا يُعامل كقرار منتج معتمد ولا يبنى عليه تسعير أو اقتصاد التطبيق قبل قرار صريح.

## 6) قرارات مرفوضة أو مستبدلة — لا تعيد اقتراحها من الصفر

- **مرفوض:** إعلانات داخل لوحة المفاتيح أو فوق المفاتيح.
- **مرفوض:** العودة إلى سير النسخ/اللصق اليدوي كالتجربة الأساسية.
- **مرفوض:** شريط سفلي مخصص يقلد أزرار Android أو يتداخل معها.
- **مرفوض:** نسخ Gboard حرفيًا؛ هي مرجع UX فقط.
- **مرفوض بعد Patch 33:** fallback ثابت ومتكرر `مرحبا/السلام/شكرا` عند غياب الذاكرة.
- **مستبدل:** Patch 28 سمحت بحقل يتمدد حتى أربعة أسطر؛ الاتجاه الأحدث هو نافذة مضغوطة بسطرين مرئيين مع تمرير للمحتوى الأطول.
- **مستبدل:** Patch 28 كان يحتفظ بمسودة التحويل بعد الإغلاق؛ Patch 30 اعتمد مسح المسودة عند الإغلاق.
- **غير معتمد:** جعل الثيمات تبويبًا مستقلًا؛ تبقى إعدادات المظهر داخل قسم الإعدادات في المرحلة الحالية.
- **غير معتمد:** Premium/اشتراك أو قيمة ثابتة مبنية على `CoinManager`; الكود الحالي ليس قرارًا تجاريًا.

## 7) المؤجل بعد الأساس

لا يفتح أي من التالي على حساب إكمال Patch 34 ثم محرك الإصلاح الحقيقي:

- إعادة تصميم Tablet adaptive بالكامل؛ المقارنة مع Gboard أظهرت أن تخطيطنا ما زال قريبًا من لوحة هاتف ممدودة.
- إعادة بناء شريط الأدوات لعرض أكبر عدد عملي من الأيقونات مع إبقاء أداة واحدة على الأقل في صفحة «المزيد».
- إعادة بناء Workspace الإيموجي: شبكة بلا إطارات ثقيلة، Scroll عمودي، فئات واضحة، رجوع وحذف ثابتان.
- Clipboard history محلية آمنة؛ الموجود حاليًا مجرد قراءة للحافظة الحالية، وليس مدير حافظة كاملًا.
- عند بناء الحافظة الكاملة: نصوص أولًا، تثبيت/حذف/مسح، حذف تلقائي لغير المثبت، وعدم إرسال المحتوى للخارج.
- نظام تقييم/Feedback محايد بعد تجربة ناجحة؛ لا Review Gating يوجه الراضين فقط إلى المتجر.
- استكمال إعدادات الصوت والاهتزاز والتباين العالي وربطها فعليًا؛ لا تعرض خيارات وهمية على أنها مكتملة.
- ربط Rewarded Ad واعتماد اقتصاد الاستخدامات.
- Firebase/Analytics/Crashlytics عند الحاجة، مع أحداث وصفية فقط ودون محتوى كتابة.
- تحسين `OnBackInvokedCallback` المسجل سابقًا كتوافق Android.
- تجهيز Release signing الحقيقي؛ `android/app/build.gradle.kts` ما يزال يحتوي إعداد Release بمفتاح Debug مؤقتًا.

ملاحظة سياقية: نقاش الدومين/Privacy/Terms بتاريخ 2026-08-08 سحبه المستخدم صراحة لأنه كان موجّهًا لمساعد مشروع آخر؛ **لا تحوّل مسار هذا المشروع نحوه باعتباره الخطوة التالية**.

## 8) المخاطر التي يجب ألا تضيع

- أخطر فجوة منتجية: `CorrectionEngine.kt` ليس Fix Arabic الحقيقي حتى الآن.
- لا تسمح للواجهة المتقدمة بأن توحي بأن ميزة الإصلاح الأساسية مكتملة قبل بناء المحرك واختباره.
- `CoinManager` يحتوي قيمًا مؤقتة قد تبدو كقرارات تجارية؛ لا تعتمدها دون قرار المستخدم.
- Android IME يحتاج اختبارات حقيقية على جهاز/محاكي؛ نجاح Flutter Analyze/Widget Tests لا يثبت سلوك نافذة الـIME أو شريط النظام.
- فرع Patch 34 متشعب عن `main` بسبب Workflows مؤقتة؛ عند الإغلاق/الدمج يجب حل الحالة بأمان وعدم فقد تغييرات المنتج.
- النصوص والحافظة بيانات حساسة؛ تبقى محلية ولا تدخل السجلات التحليلية.
- تراخيص القواميس جزء من المنتج ولا تحذف أثناء إعادة البناء.

## 9) خريطة المراجع المرتبطة

| المرجع | الدور | ما يجب قراءته عند الحاجة |
|---|---|---|
| `nickolaylo/general-work-charter` — `main` | المرجع العام لطريقة العمل | ابدأ من `AI_START_HERE.md` ثم `INDEX.md`; دائمًا عند متابعة المشروع: `rules/core-workflow.md` و`rules/git-patches-and-project-state.md`; بوابة الجورنال: `rules/project-journal-entry-gateway.md` / GWC-061 |
| `nickolaylo/fixarabickeyboard` — `main` | Final Base ومصدر المنتج المعتمد | هذا الجورنال + Git/PRs الفعلية |
| `agent/patch-34-keyboard-personalization-and-compose-viewport` | العمل المرشح الحالي | PR #13 + الملفات التسعة المتغيرة، خصوصًا `KeyboardImeService.kt` و`styles.xml` والإعدادات |
| `nickolaylo/fix_arabic2026` — `main` | المرجع العملي للتطبيق الأصلي ومحرك تحويل العربية | `lib/models/converter.dart` ثم `lib/screens/message_editing_screen_v3.dart`; استعمله مرجعًا للسلوك لا مصدرًا لنسخ كل بنية التطبيق أو الإعلانات |
| أصول القواميس داخل هذا المستودع | مصادر الاقتراحات والتراخيص | `android/app/src/main/assets/dictionaries/` و`tools/dictionaries/` و`.../licenses/` |
| Android `InputMethodService` / WindowInsets / System Navigation | مرجع سلوك الـIME والشريط السفلي | الشريط وأزراره أصلية للنظام؛ أي حل يجب احترام دورة حياة IME وInsets بدل تقليدها بواجهة مخصصة |
| صور/فيديوهات Gboard ولوحة المفاتيح الهندية التي قدمها المستخدم | مراجع UX فقط | استخلص منها الارتفاع/الحجم/المسافات/تنظيم الأدوات، ولا تعتبرها مواصفات نسخ حرفي أو مستودعًا تابعًا للمشروع |

### ما ليس ارتباطًا فعليًا حاليًا

- لا توجد في `pubspec.yaml` الحالي تبعيات AdMob أو Firebase.
- لا يوجد ارتباط كودي مثبت حاليًا بكتالوج Jana Apps أو مستودع Web داخل هذا المشروع؛ لا تفترضه أو توسع النطاق إليه قبل تنفيذ ميزة تحتاجه فعلًا.

## 10) ملخص المراحل المقبولة الحديثة

- **Patch 24:** الأساس البصري للتطبيق المرافق.
- **Patch 25:** الأساس البصري للوحة Android الأصلية.
- **Patch 26:** Magic Compose + Smart Toolbar.
- **Patch 27:** المسافات/التكثيف/بطاقات الاقتراح.
- **Patch 28:** إغلاق مستقل + Multiline أولي.
- **Patch 29:** صفحة المزيد ثابتة + Compose مضغوط.
- **Patch 30:** صقل محرر Compose، مسح المسودة عند الإغلاق، اقتراحات عند المؤشر.
- **Patch 31:** تحسين الأداء + Emoji Panel + Manual Lines + إعداد صف الأرقام.
- **Patch 32:** حذف داخل الإيموجي + ضغط الصفوف.
- **Patch 33:** اقتراحات Cold-Start سياقية واحترام جهة بطاقات الذاكرة — **Final Base الحالية**.
- **Patch 34:** تخصيص الارتفاع/الحروف/المسافة/الحدود + Multiline Viewport + Safe Insets — **Candidate نشطة غير معتمدة**.

كل ما قبل Patch 33 تاريخ مدمج، وليس قواعد بناء منفصلة.

## 11) بروتوكول الاستئناف لمساعد جديد

عند قول المستخدم «راجع جورنال المشروع» أو فتح شات جديد:

1. اقرأ هذا الملف كاملًا.
2. اقرأ `AI_START_HERE.md` و`INDEX.md` من الميثاق المركزي، ثم القواعد المرتبطة بالمهمة؛ `GWC-061` مطبقة على هذا الجورنال.
3. تحقق من `main` وPR #13 والفرع النشط؛ لا تعتمد الـSHA أو الحالة المكتوبة هنا إذا تغير Git بعدها.
4. إذا كانت المهمة تخص الإصلاح الحقيقي، افتح مباشرة `CorrectionEngine.kt` ومرجع `fix_arabic2026/lib/models/converter.dart` قبل الاقتراح أو التعديل.
5. إذا كانت تخص القواميس، راجع ملفات القاموس والتراخيص المحلية.
6. إذا كانت تخص الإعلانات/التحليلات، تذكر أنها غير مربوطة فعليًا في هذه النسخة وافتح قواعد Ads/Privacy من الميثاق قبل التنفيذ.
7. تابع من آخر نقطة صحيحة؛ لا تعيد اقتراح القرارات المرفوضة أعلاه ولا تغير الطريق بصمت.
