# PROJECT_JOURNAL

## Final Base الحالية

- `Patch 16 — Live Dictionary Suggestions While Typing` هي Final Base الحالية بعد تأكيد المستخدم: «تم التطبيق بنجاح».

## المرحلة الحالية

- تثبيت التحديث الحي لاقتراحات القاموس أثناء كتابة كل حرف، قبل الانتقال إلى قواميس اللغات الأخرى.

## القرارات المعتمدة

- جميع اللغات تستخدم `SuggestionEngine` و`DictionaryManager` نفسيهما.
- القاموس العربي الموسع ومشتقاته يبقيان كما هما من Patch 14.
- الخانة الأولى المتعلمة محليًا نجحت عمليًا: بعد «السلام» تقترح «عليكم»؛ هذا السلوك معتمد ولا يُغيّر.
- الأرقام والحقول الحساسة لا تدخل التعلم المحلي.

## Patch 16 — Live Dictionary Suggestions While Typing

- إصلاح تحديث شريط الاقتراحات بعد كل حرف مباشرة بدل انتظار المسافة.
- استعمال النص المحلي الأحدث فورًا، ثم مزامنته مرة ثانية بعد أن يعتمد محرر أندرويد الحرف.
- عدم تغيير القاموس أو التعلم المحلي أو البريد أو التصميم أو شريط الإصلاح.

## الاختبار المطلوب

- كتابة `خ` ثم `خط` ثم `خطي` والتأكد من تحديث الاقتراحات بعد كل حرف دون ضغط المسافة.
- الحذف حرفًا حرفًا والتأكد من تحديث الاقتراحات مباشرة.
- كتابة `السلام` والتأكد من بقاء `عليكم` في الخانة الأولى.


## Patch 17 — Android Packaged Dictionary Asset Resolution

- أكد فحص APK أن `index.tsv` يشير إلى ملفات تنتهي بـ `.txt.gz`، بينما حزمها Android داخل APK بأسماء تنتهي بـ `.txt`.
- تم إصلاح `DictionaryManager` مركزيًا ليقرأ الشكلين، ويكتشف من ترويسة المحتوى هل البيانات مضغوطة فعلًا قبل فكها.
- لا تغيير في ترتيب الخانات أو التعلم المحلي أو البريد أو تصميم لوحة المفاتيح.
- الاختبار: كتابة `خ` ثم `خط` ثم `خطي` والتأكد من ظهور اقتراحات القاموس فورًا دون ضغط المسافة.
