# PROJECT_JOURNAL

## Final Base الحالية

- `Patch 21 — Complete English Dictionary Integration` هي Final Base المعتمدة قبل هذا الباتش.
- بعد نجاح الاختبار واعتماد المستخدم، تصبح النسخة الجديدة:
  `Patch 22 — Complete French Dictionary Integration`.

## المرحلة الحالية

- إضافة القاموس الفرنسي إلى محرك الاقتراحات المركزي بعد استقرار العربية والإنجليزية والتعلّم المحلي.

## القرارات المعتمدة

- قاموس فرنسي محلي ومنظف ومرتب حسب الشيوع، مع فصل ملفاته وتعلّمه عن بقية اللغات.
- الحفاظ على الكتابة الفرنسية الصحيحة، مع البحث دون لكنات: `ecole → école` و`etre → être` و`coeur → cœur`.
- توحيد أشكال الفاصلة العليا ودعم الكلمات المركبة والاختصارات الكاملة مثل `l'école` و`j'aime` و`qu'il`.
- اعتماد 26 حزمة مضغوطة فقط، مع فهرس صغير للكلمات الشائعة بعد كتابة حرف واحد.
- توثيق مصدر FrequencyWords وترخيص المحتوى `CC BY-SA 4.0` وترخيص الكود `MIT` داخل أصول القاموس.
- لا تغيير في القاموس العربي أو الإنجليزي أو التعلّم المحلي أو شريط الإصلاح أو تصميم لوحة المفاتيح.

## Patch 22 — Complete French Dictionary Integration

- دمج `96,703` مدخلات فرنسية منقّاة من `fr_50k.txt` و`fr_full.txt` مع استعمال `fr_ignored.txt` في الاستبعاد.
- إضافة طيّ اللكنات الفرنسية مركزيًا مع الإبقاء على الكلمة الصحيحة عند العرض، ودعم `œ → oe` و`æ → ae`.
- تحسين سياق الفاصلة العليا حتى تبقى `l'` و`j'` و`qu'` جزءًا من بادئة البحث.
- إضافة فهرس شيوع خفيف للحرف الأول دون زيادة عدد ملفات الحزم أو تغيير مسار اللغات الأخرى.
